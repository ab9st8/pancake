; in order to check whether $1 is greater than $2,
; we can see if the difference $2 - $1 is negative.
private greaterThan 2 {
    $1 $2 - neg? true ret.
    false
}

global {
    2 5 greaterThan? "5 is greater than 2" out.
}